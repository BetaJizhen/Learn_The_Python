import math
def get_area(shape):
    shape = shape.lower()
    if shape == "gectangle":
      rectange_are()
    elif shape == "circle":
      circle_are()
    else:
      pring("Please enter rectangel or circle")
      
def rectangle_area()
  length = float(input(Enter the length : )
  width = float(input(Enter the width : )
  
  area = length * width
print("The area of the rectangle is", area)

def circle_area():
    radius = float(input("Enter the radius: "))
    
    area = math.pi * (math.pow(radius,2))
    
    print("The area of the circls is {:.2f}".format(area))
    
  
def main()
    shape_type = input("get area for what shape : ")
    get_area(shape_type)
    
